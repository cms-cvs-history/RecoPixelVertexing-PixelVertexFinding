process PROD = {

  source = PoolSource {
    untracked vstring fileNames = {'file:ClustersToHits.root'}
    untracked int32 maxEvents = 1000
    untracked uint32 debugVerbosity = 0
    untracked bool   debugFlag     = true
  }

  include "RecoPixelVertexing/PixelVertexFinding/data/PixelTracks.cff"  
  include "RecoPixelVertexing/PixelVertexFinding/data/PixelVertexes.cff"  

  module test = PixelVertexTest{
    string TrackCollection = "PixelTracks"
    untracked string OutputTree = "pixel_vertexes_6mu.root"
    untracked uint32 Verbosity = 1
  }

  module out = PoolOutputModule 
  {
    untracked string fileName = "reco-application-tracking-rechitconversion.root"
  }

  path p = {PixelTracks, PixelVertexes, test}

  endpath e = 
  {
    out
  }
}

